@using UniPass.Domain
@using UniPass.Client.Components.Modals.Key
@using UniPass.Client.Services.Api
@using UniPass.Client.Components.Modals.Folder
@inject DialogService DialogService
@inject FolderService FolderService

<RadzenPanelMenu>
    <RadzenPanelMenuItem IconColor="@Colors.Secondary" Text="Команды">
        <RadzenPanelMenuItem IconColor="@Colors.Secondary" Text="Мои" Path="/my-teams"/>
        <RadzenPanelMenuItem IconColor="@Colors.Secondary" Text="Участие" Path="/teams"/>
    </RadzenPanelMenuItem>
</RadzenPanelMenu>

<hr class="rz-b-1" style=" width: 70%;">
<RadzenPanelMenu>
    <RadzenPanelMenuItem IconColor="@Colors.Secondary" Text="Личные" Path="/my-keys"/>
</RadzenPanelMenu>

<span class="material-symbols-outlined">

</span>
@code
{
    private async Task ShowCreateKey()
    {
        var result = await DialogService.OpenAsync<CreateFolderModal>($"Создание папки",
            new Dictionary<string, object>() {  },
            new DialogOptions() { Width = "auto", Resizable = true, Draggable = true });
    }
}